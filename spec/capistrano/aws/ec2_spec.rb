# frozen_string_literal: true

RSpec.describe Capistrano::Aws::Ec2 do
  it 'has a version number' do
    expect(Capistrano::Aws::Ec2::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
